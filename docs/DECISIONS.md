# Decision Log

One entry per research decision. Raw material for the Research Note and AI Usage Note.

| # | Decision | Options considered | My choice | Claude's view | Why I chose it / what I checked |
|---|----------|--------------------|-----------|---------------|----------------------------------|
| 1 | Data source | A: niftyindices.com · B: yfinance ^NSEI · C: both, cross-checked | A — niftyindices.com (NSE official) | Agreed; suggested optional yfinance cross-check as a validation step only; flagged risk that older Open prices may be unreliable | Official/primary source is most defensible for quant research methodology. UI limits queries to 1 year (client-side check only); `src/fetch_data.py` calls the site's own backend endpoint and downloads the full range in one request. Raw response saved unmodified to `data/raw/` for reproducibility. |
| 2 | Sample start date | A: 2000→today · B: 2005→today · C: 2008→today | A — 2000-01-01 → latest available | Agreed; noted it also gives the development (in-sample) period multiple crashes (2000–01, 2008), so the out-of-sample split doesn't leave development with only one regime | More observations and multiple market regimes (2000–01, 2008, 2020). Chosen on research coverage, before seeing any results — not on historical performance. |
| 2.1 | Calendar gaps | A: accept & document · B: verify vs NSE holiday list · C: cross-check vs yfinance dates | A — accept, document, don't fill/remove | Agreed; added a trading-days-per-year check (243–254 every full year) because a gap check can't detect a single missing weekday | 12 gaps > 4 days, all 5–6 days, consistent with holiday + weekend. Filling would invent prices; removing nothing to remove. |
| 2.2 | Invalid rows | A: fail loudly · B: drop + warn | A — raise error | Agreed; added unit tests proving the validator raises | Silent dropping hides data problems. None found in the actual data. |
| 2.3 | Unreliable pre-2011 Open | A: use anyway · B: next-day Close entry · C: Open only from 2011 · D: Open main + Close robustness | D — next-day Open main, next-day Close robustness; raw data untouched | Agreed; added per-row `suspicious_open` flag so each event shows whether its entry may be stale | 2000–2010: Open = prev Close on 2–11% of days, Open at day's High/Low on 11–26% (vs ~0% and 2–5% after 2011). Test whether this materially changes results rather than deleting a decade of data. |
| 2.4 | Extreme days | A: keep all · B: keep + sensitivity excluding largest · C: exclude/winsorize | B — keep; sensitivity excluding the largest few | Agreed; proposed fixing "few" now (before results) = 5 events with largest absolute forward return | Extreme days are real events (2004 election, 2008, COVID) and are exactly what the hypothesis is about; sensitivity checks whether a handful of observations drive the result. |
| 3.1 | Significant fall | A: fixed ≤ −2% · B: volatility-scaled · C: both | B — close-to-close return < −2 × std of previous 60 trading days' returns (t−60…t−1, no look-ahead). Robustness: 1.5×, 2.5×, fixed −2%, 20-day window | Agreed on B/2×. Corrected my initial reasoning: the rule scales by the *size* of recent moves (volatility), not the *direction* of the trend — a slow steady decline has low vol, so a sudden −2% day still counts. Explained 2× ≈ 2.3% of days under normality (~6/yr) vs 1.5× ≈ 6.7% (~17/yr, too common to be "significant") | Event counts (not outcomes) showed fixed −2% puts 61 events in 2008 alone and 0 in 2017/2023 — it measures "turbulent years", not "unusual falls". Scaling makes "significant" mean unusual relative to current conditions. 2× gives 214 events — enough to test; 1.5×/2.5× either side checks the result isn't threshold-specific. |
| 3.2 | Recovery definition | A: forward return > 0 · B: forward return > baseline · C: regains event-day loss · plus: main test = retracement (X) vs forward return (Y); daily High vs Close | **Main: 50% retracement using daily Close.** loss = prev-day Close − event-day Close; recovery_level = event-day Close + 0.5 × loss; recovered if any daily Close within the holding period ≥ recovery_level (20,000 → 19,000 ⇒ level 19,500). **Baseline:** same measurement on non-event days (days not meeting the 2× vol fall rule); compare event vs baseline recovery rate (e.g. 60% vs 45%), never the event rate alone. **Robustness:** 100% retracement (Close back to prev-day Close). **Separate tradable analysis:** forward returns from the specified entry price, computed independently (recovery can happen overnight before a tradable entry). | Raised: (1) no baseline ⇒ a ~60% rate could be pure chance (random-walk estimate 55–65% for a +1% touch within 5 days); (2) High vs Close ambiguity; (3) recovery ≠ tradable return because entry is next-day Open. All three addressed in final definition. Open sub-question: how far a baseline day must rise, since a normal day has no 'loss' (see 3.2a) | 50% measure is the intuitive main definition of 'recover'; Close avoids counting brief intraday touches and matches the close-based event rule; baseline stops a high recovery rate being read as evidence if ordinary days recover as often; 100% tests a stricter definition; forward returns address tradability separately. |
| 3.2a | Baseline recovery target for non-event days (a normal day has no "loss") | A: +1σ (target after a minimum 2σ fall) · B: same % rise as each event · C: same rise in volatility units | **C — volatility-normalised target.** For each event i: k_i = (recovery_level_i / event_close_i − 1) / σ_i. For each normal day j in the same sample period: target_j = close_j × (1 + k_i × σ_j), hit if any Close within N days ≥ target_j. Event i's baseline rate = share of normal days that hit; overall baseline = average across events. σ = same 60-day window as the event rule. | Agreed. Noted A is simplest but biased against the hypothesis (events are usually > 2σ, so their targets are further away); B ignores volatility regime (+2.6% is easy in 2008, hard in 2017) | The event rule is defined in volatility units (2σ), so the recovery comparison should be too; keeps event vs baseline comparable across volatility regimes. |
| 3.3 | Main holding period N | 1 / 3 / 5 / 10 trading days | **N = 5.** Event = day t; window = Closes of t+1 … t+5 (next 5 trading sessions). Same N for recovery window and forward-return horizon. Robustness: N = 1, 3, 10. Window is fixed from the event date — never "wait for the first up day, then start the clock". | Agreed; 5 ≈ one trading week is the closest match to "next few trading days"; pre-committing one main N guards against tuning the horizon | Matches the hypothesis wording. Rejected a "start after first up day" variant: it conditions on post-event price action (a selection/timing rule chosen after seeing the path), which would bias recovery upward. |
| 3.4 | Exit rule | A: Entry Open(t+1) → Exit Close(t+5) · B: Entry Open(t+1) → Exit Open(t+6) | **A — entry Open(t+1), exit Close(t+5).** B kept as a robustness check | Gave a neutral comparison, no recommendation. Corrected the premise that B gives "exactly five sessions": A spans exactly t+1…t+5, B adds the t+5→t+6 overnight. Also noted A uses one fragile price instead of two (pre-2011 entry Opens flagged for 23.9% of events vs 1.6% after 2011), and that B would silently degenerate into A whenever the Open is a stale copy of the previous Close | The recovery analysis covers the five sessions t+1…t+5; exiting at Close(t+5) keeps the trading-return measurement aligned with exactly that window. B adds an overnight gap beyond the predefined window, so recovery and return would cover different periods. |
| 3.5 | Overlapping events | A: count all 214 as independent · B: non-overlapping filter (159 events) · C: keep all, adjust inference. Episode rules compared: 1a gap > 5 trading days · 1b/1c gap > 10/21 days · 2 calendar month/quarter · 3 drawdown episode | **C with Rule 1a.** Keep all 214 events; each keeps its own recovery target, window, outcome and forward return — no merging. Group events into episodes (new episode when > 5 trading days since previous event) and use episodes only for uncertainty (cluster/block resampling). Robustness: gap > 10, > 21, and the naive independent assumption | Provided three objective price/date-based episode rules with counts and the resulting CI widening (1a: 146 episodes, ×1.2; month: 132, ×1.3; quarter: 77, ×1.7; drawdown: 49, ×2.1). Flagged that drawdown episodes merge all of 2000–03 into one 30-event observation, and that calendar boundaries split the COVID crash. No recommendation given. Reminded that baseline days overlap identically and need the same treatment | The statistical problem is overlapping 5-session measurement windows, so the episode rule should match that horizon rather than a calendar convention or a multi-year drawdown. Keeping all events preserves every observation and the point estimates; clustering only affects uncertainty, which is where the dependence actually matters. 2020 contributes 8 events describing one episode — they should not count as 8 independent pieces of evidence. |
| 3.6 | Baseline set for forward returns | A: all days · B: non-event days · C: volatility-matched · D: σ-normalised returns | **B — non-event days** (all days with a valid 60-day σ that do not meet the 2σ fall rule; 6,369 of 6,583 usable days) | Agreed, but challenged the size of the gain: events are only 3.3% of days, so A and B give nearly identical numbers (A understates the gap by ~3.3%). Argued B should be justified as consistency with the 3.2 recovery baseline, not as fixing a large bias. Noted volatility matching (C) adds little here because the 2σ rule already spreads events evenly across volatility quintiles (49/48/42/39/36, median σ 0.96% on events vs 1.04% on all days) | Cleaner event vs non-event comparison; keeps the forward-return baseline consistent with the recovery baseline already defined in 3.2/3.2a. |
| 3.6a | Baseline contamination by post-event days | A1: keep post-event days · A2: exclude days whose window overlaps an event window | **A1 main, A2 as robustness check** | Agreed. Measured the contamination: 856 baseline days (13.4%) fall within 5 days after an event, 1,501 (23.6%) have overlapping windows. Noted the bias direction is conservative — if the effect is real, those days inherit part of it, lifting the baseline and shrinking the measured gap. Noted A2 would remove 23.6% of days concentrated in turbulent periods, tilting the baseline toward calm markets — a larger unknown bias in exchange for a small known one | "Normal market behaviour" should include days that follow falls; excluding them would redefine the baseline. The contamination works against the hypothesis, so it cannot manufacture a positive result. |
| 3.6b | Dependence in overlapping 5-day returns | 1: moving block bootstrap (events and baseline separately) · 2: stationary bootstrap · 3: whole-series block resampling, recomputing both sides · 4: Newey–West HAC standard errors | **3 — whole-series block bootstrap.** Point estimates stay simple averages (event mean, baseline mean, difference in percentage points). Each replicate resamples contiguous blocks of days carrying their labels/outcomes, recomputes event_mean − baseline_mean, and the distribution of those differences gives the 95% CI. Block length: **L = 10 trading days** (main), with L = 5 and L = 21 as robustness checks. | Presented four options with assumptions and trade-offs, no recommendation. Key points: overlap correlation measured at 0.80 / 0.58 / 0.37 / 0.16 / −0.02 for lags 1–5, confirming dependence is mechanical and dies exactly at the window length; bootstrapping the two sides separately would assume they are independent, which is false since both come from one timeline; event side dominates uncertainty ~3:1 so all options give similar widths | The conclusion rests on the *difference* between event and baseline returns, so the confidence interval should be built around that difference directly. One resampling mechanism covers both sides instead of episode clustering for events plus block resampling for the baseline. Bootstrap also handles the fat tails of 5-day index returns better than an analytic formula. **Block length:** 10 = 2x the 5-day return window, giving margin for residual dependence without being unnecessarily long; measured autocorrelation is ~0 by lag 5, so 10 covers the mechanical overlap plus slack; 5 (minimum, slightly overconfident) and 21 (~1 trading month, conservative) reported alongside so the conclusion does not hinge on L. |
| 3.7 | Development / out-of-sample split | A: 2000–2017 / 2018–2026 (142 / 72 events) · B: 2000–2014 / 2015–2026 (118 / 96) · C: 2000–2019 / 2020–2026 (~166 / ~48) | **A — development 2000-01-01 to 2017-12-31, out-of-sample 2018-01-01 onwards.** All rules frozen before the OOS period is evaluated; OOS is run once and its results must not feed back into any methodology choice | Agreed. Noted that only event *counts* and *data-quality* statistics have been inspected across the full sample so far — never outcomes — so the OOS period is genuinely unseen. Also noted the unreliable pre-2011 Opens all sit in the development period under every split, so OOS uses clean prices | Conventional ~2/3 – 1/3 chronological split; 142 development events is a solid base and 72 OOS events is enough to be meaningful. 2018–2026 spans several regimes (COVID crash and recovery, 2022 bear, recent calm years), so it tests persistence across conditions rather than one regime. |
| 3.8 | Transaction costs | 0.05% · 0.10% · 0.20% round trip | **0.10% round-trip as the main assumption; 0.05% and 0.20% as sensitivity.** Wording requirement: 0.10% must be described as a **modelling assumption for total round-trip trading friction** (brokerage, exchange fees, STT, stamp duty, GST, bid-ask spread and slippage), never as a measured or historical average NIFTY transaction cost — an OHLC dataset cannot support such a claim. Actual friction depends on the tradable instrument (futures / ETF), broker, trade size, taxes and execution quality. NSE market-impact/slippage material may be cited as context only | Agreed; provided instrument-level ranges (futures ≈ 0.03–0.06% round trip, NIFTYBEES ≈ 0.15–0.30% where delivery STT dominates) and flagged that entering at the Open and exiting at the Close are the widest-spread, most volatile moments of the session, so the low end of any range is optimistic for this design | 0.10% is a defensible middle assumption for futures with realistic slippage at Open/Close; the sensitivity range shows whether any measured edge survives friction. ~6 events/year implies ~0.6%/year in costs, so the decisive question is whether the edge is comfortably larger than the assumed friction. |
| 3.9 | Statistical method | Main test fixed by 3.6b (whole-series block bootstrap, L=10, 95% CI on event_mean − baseline_mean). Sub-choices: (a) secondary return test (Welch t / Newey–West / none) · (b) recovery-rate test (bootstrap / two-proportion z / both) · (c) one- vs two-sided · (d) multiple-testing policy | **(a) Newey–West** regression of the 5-day forward return on an event dummy (~5 lags) as the secondary check. **(b) The same whole-series block bootstrap** for the recovery-rate difference. **(c) Two-sided.** **(d) Pre-specified main specification only** carries the formal conclusion; all other thresholds, horizons, block lengths and cost levels are robustness/sensitivity, not evidence | Agreed on all four. Noted a Welch t-test would ignore the overlap and give optimistic p-values, which is why Newey–West is the better secondary check; and that a two-proportion z-test assumes independence already shown to be false | Consistent treatment of dependence across both metrics; two-sided also allows detecting the opposite result (falls followed by worse-than-normal returns), which would itself be a finding; reporting one pre-registered specification prevents cherry-picking from ~50 robustness variants where ~1 in 20 would look significant by chance. |
| REJECT | Pre-registered rejection criterion | — | **Reject the hypothesis if** the out-of-sample results do not show a positive recovery / forward-return advantage over the non-event baseline, **or** if the observed advantage is not reasonably robust to the pre-specified sensitivity checks | Flagged that "reasonably robust" is not yet quantified — see question raised before implementation | Written before any outcome was computed. Commits the conclusion to out-of-sample evidence plus stability across pre-specified variations, rather than to an in-sample result at one parameter setting. |
| 8 | Regime cuts (Step 8, descriptive only) | Volatility median / trend / sub-periods | **Confirmed as run:** calm vs turbulent = 60-day σ at or above its median; uptrend vs downtrend = Close vs 200-day MA; sub-periods 2000–2007, 2008–2013, 2014–2019, 2020–2026. Descriptive only; cuts must not be re-optimised after seeing results | Presented the cuts and results together and asked for confirmation rather than treating them as settled | Objective, standard cuts fixed before interpretation; no conclusion rests on them. |
| 9 | Backtest handling of events during an open trade | A: skip events while in a trade (non-overlapping, one position) · B: take every event | **A is the main backtest; B reported as a secondary challenge result, with the difference between them stated openly** | Flagged that the two modes disagree in sign out-of-sample (+6.64% vs −30.67% at 0.10% cost) because mode A skips the repeated COVID-crash events, which are the worst trades — so mode A's result is flattered by the overlap rule. Asked for the decision rather than choosing | One position at a time is the simplest realistic implementation and keeps the equity curve interpretable. Mode B is retained precisely because the gap between the two exposes how much the result depends on this rule. |
