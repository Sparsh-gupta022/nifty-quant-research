# Raw data

Source: NIFTY Indices (NSE Indices Ltd), https://www.niftyindices.com/reports/historical-data
Index: NIFTY 50 (price index), daily OHLC.
Downloaded with `python src/fetch_data.py` (one request; the site UI's 1-year limit is a client-side check only).
Files are committed unmodified; all cleaning happens in code.
